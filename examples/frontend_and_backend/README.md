# Frontend and Backend Worktree Example

Example polyrepo with separate backend and frontend worktrees. The frontend project declares a dependency on the backend project, which needs to be created first.
