name: "some-project-%STACK_NAME%"

services:
  app:
    ports: !override
      - "%APP_PORT%:80"
  postgres:
    ports: !override
      - "%POSTGRES_PORT%:5432"
  redis:
    ports: !override
      - "%REDIS_PORT%:6379"
